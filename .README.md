# Multi-Tenant Project Management Platform (Jira-lite)

Production-grade full-stack SaaS:
- Spring Boot + PostgreSQL
- React + TypeScript (Vite)
- JWT Auth + RBAC
- Tenant isolation (workspace-based)
- CRUD: projects, tasks, comments
- Pagination, filtering, search
- Docker + CI

## Local Dev
### 1) Start Postgres
```bash
docker compose up -d
